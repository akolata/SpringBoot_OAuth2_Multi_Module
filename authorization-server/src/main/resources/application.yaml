server:
  port: 8081
spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3306/oauth_tests
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root